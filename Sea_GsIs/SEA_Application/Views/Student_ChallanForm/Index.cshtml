@model IEnumerable<SEA_Application.Models.Student_ChallanForm>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AmountPayable)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AspNetStudent.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Class_ChallanForm.Title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AmountPayable)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetStudent.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Class_ChallanForm.Title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
